import { useEffect } from "react";
import "../style/TypePoke.css"

function TypePokemon(props) {
  /*useEffect(()=>{
    localStorage.setItem('Ipssi', 'PX');
  },[])*/
    let types = props.types
    return (
      <>
      </>
    );
  }
  
  export default TypePokemon;